<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Profile</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <a href="/logout" class="bg-red-500 mb-5 inline-block px-3 py-2 rounded-md">Log out</a>
        <div class="flex gap-5 items-center">
            <div class="h-20 w-20 rounded-md">
                <img class="h-full w-full objectfit-cover rounded-md" src="/images/uploads/<%=userData.profile %>" alt="">
            </div>
            <h1 class="text-4xl mb-6">Hello, <%= userData.name %> 👋 </h1>
        </div>
            <div class="createPost mb-6">
                <form action="/post" method="post" class="flex flex-col">
                    <textarea 
                    name="content" 
                    class="w-1/3 p-3 resize-none outline-none border-2 border-zinc-700 mb-3 rounded-md"
                    placeholder="What's on your mind today"
                    ></textarea>
                    <input type="submit" value="Create your post" class="bg-blue-500 px-3 py-2 rounded-md w-fit">
                </form>
            </div>

            <hr>

            <div class="postContainer p-3 grid grid-cols-4 gap-2">
                <% userData.posts.slice().reverse().forEach(function(post) { %>
                    <div class="post p-3 bg-zinc-600 p-3 rounded-md">
                        <h3 class="mb-3 text-blue-400">@<%= userData.username %> </h3>
                        <p class="text-zinc-300"><%= post.content %></p>
                        <small class="inline-block mt-3"><%= post.likes.length %> likes</small>
                        <div class="btns flex justify-between mt-2">
                            <a href="/like/<%= post._id %>" class="text-pink-500">
                                <%= post.likes.indexOf(userData._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a href="/update/<%= post._id %>" class="text-zinc-400">Edit</a>
                            <a href="/post/delete/<%= post._id %>" class="text-red-500">Delete</a>
                        </div>
                    </div>
                <% }) %>
            </div>
            
    </div>
</body>
</html>