<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Profile</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h1 class="text-4xl mb-6">Edit your post</h1>
        <div class="createPost mb-6">
            <form action="/update/<%= post._id %>"" method="post" class="flex flex-col">
                <textarea name="content"
                    class="w-1/3 p-3 resize-none outline-none border-2 border-zinc-700 mb-3 rounded-md"
                    placeholder="What's on your mind today"><%= post.content %></textarea>
                <input type="submit" value="Update your post" class="bg-yellow-500 px-3 py-2 rounded-md w-fit">
            </form>
        </div>
    </div>
</body>

</html>